; ===================================================================
;  Queues — solo defaults; las colas reales se gestionan vía ARI/AMI
;  desde el backend (módulo Queues).
; ===================================================================
[general]
persistentmembers = no
autofill = yes
monitor-type = MixMonitor
shared_lastcall = yes
log_membername_as_agent = yes
